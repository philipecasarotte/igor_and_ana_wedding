<li id="page_<%= page.id %>">
	<% if page.children.size > 0 %>
		<%=link_to image_tag("act_as_tree/open.gif", :size => "20x16", :alt => ""), admin_pages_url(:parent_id => page, :format => :js), :class => "first" %>
	<% else %>
		<%= image_tag "act_as_tree/simple.gif", :size => "20x16", :alt => "" %>
	<% end %>
	<span><%=h page.name %></span>
	<div>
		<%=link_to 'Reordenar filhas', reorder_admin_pages_url(:parent_id => page.id) if page.children.size > 1  %>
		<%=link_to 'Adicionar filhas', new_admin_page_url(:parent_id => page.id) %>
		<%=link_to 'Editar', edit_object_url(page) %>
		<%=link_to 'Deletar', object_url(page), :confirm => 'Você tem certeza?', :method => :delete unless page.is_protected %>
	</div>
</li>
